# TFM Module — Instructions

## Purpose

Master's thesis (TFM) experiment runner.  This module is the **top-level entry
point** for batch forecasting experiments and EDA figure generation.  It sits
above the core `forecast/` engine and delegates all computational work to
canonical modules (`src.forecast.pipeline`, `src.data.data_fetcher`,
`src.forecast.optimization`).

## File Inventory

| File | Role |
|------|------|
| `tfm_forecast.py` | Experiment orchestrator — wires dataset configs and strategy params, then calls `run_forecast_pipeline()` |
| `tfm_plots.py` | EDA figure runner — iterates datasets and calls `src.analysis.dataset_plots` functions |

## Key Functions

### `tfm_forecast.py`

| Function | Description |
|----------|-------------|
| `get_dataset_config(dataset_name, hussain=False)` | Returns `(split_date_str, ranges_dict)` with train/test/zoom date ranges per dataset |
| `execute_lstm(datasets, li_forecast_horizons, …)` | Run LSTM forecasts for each dataset × horizon |
| `execute_transformer(…)` | Run Transformer forecasts |
| `execute_lightgbm(…)` | Run LightGBM forecasts |
| `execute_xgboost(…)` | Run XGBoost forecasts |
| `execute_hybrid(…)` | Run Hybrid LSTM-Transformer forecasts |
| `execute_dl_baseline_lstm(…)` | LSTM with Hussain et al. (2025) hyperparams |
| `execute_dl_baseline_transformer(…)` | Simplified Transformer from Hussain et al. |
| `execute_dl_baseline_hybrid(…)` | Hybrid with Hussain et al. hyperparams |
| `optimize_lstm(datasets, n_trials, …)` | Optuna hyper-parameter search for LSTM |

### `tfm_plots.py`

| Function | Description |
|----------|-------------|
| `main()` | Generates EDA figures (trends, decomposition, boxplots) for configured datasets |

## Hussain et al. (2025) Reproduction

Three `execute_dl_baseline_*` functions reproduce the architectures and
hyperparameters described in Hussain, A. et al. "Charging stations demand
forecasting using LSTM based hybrid transformer model" *Sci Rep* 15, 13555
(2025).

Key differences from our default models:
- `look_back = forecast_horizon` (vs. `max(MIN_LOOK_BACK, forecast_horizon)` for our models)
- `dropout = 0.2` (vs 0.1 default)
- `ReduceLROnPlateau` + `EarlyStopping` callbacks
- COVID data **included** (`exclude_covid=False`) to match article methodology
- Prediction uses schedule (multi-step recursive) mode

## Canonical Imports

All logic has been refactored into reusable modules:

| Import | Source module |
|--------|-------------|
| `run_forecast_pipeline()` | `src.forecast.pipeline` |
| `fetch_daily_energy_for_forecast()` | `src.data.data_fetcher` |
| `objective_lstm()`, `objective_transformer()` | `src.forecast.optimization` |
| `Colors` | `src.utils.console` |

## Conventions

- Logging via `logging.basicConfig()` (standalone, not the framework `Logger`).
- ANSI color highlighting via `Colors` from `src.utils.console`.
- Output plots go to `output_plots/` at project root.
- `tfm_plots.py` uses `matplotlib` (not Plotly).
- `MIN_LOOK_BACK = 14` — module-level constant; non-Hussain neural models use `look_back = max(MIN_LOOK_BACK, forecast_horizon)`, so look_back is at least 2 weeks but grows with the horizon.
- `__main__` block configures datasets, horizons, and `split_date_str`, then
  calls the `execute_*` functions sequentially.

## Known Issues

- **`optimize_lstm()` free variables**: Uses `split_date_str` and
  `li_forecast_horizons` which are only defined in the `if __name__ ==
  "__main__"` block — calling this function from outside `__main__` will raise
  `NameError`.  Fix: pass them as parameters.
- **Hardcoded paths in `tfm_plots.py`**: Figure output path and DB credentials
  are hardcoded.  Consider using config or environment variables.

---

## Experiment Matrix — Datasets × Models × Horizons

### Our models (non-Hussain)

| Dataset | Horizons tested | Notes |
|---------|----------------|-------|
| ACN_Caltech | 1, 7, 30, 120 | COVID excluded via `exclude_range` |
| ACN_JPL | 1, 7, 30, 120 | COVID excluded via `exclude_range` |
| ACN_Office001 | 1, 7, 30, 120 | No COVID exclusion needed (split avoids gap) |
| Dundee | 1, 7, 30, 120 | Tariff shock context (2024); training ends 2023-09-01 |
| BeLib | 1, 7 | Very short data (~1 month); longer horizons not meaningful |
| AMB_Barcelona | 1, 7, 30 | ~1 year of data |

### Hussain et al. reproduction

| Dataset | Horizons tested | Notes |
|---------|----------------|-------|
| ACN_Caltech | 30, 120, 240 | COVID **included** (article methodology) |
| ACN_JPL | 30, 120, 240 | COVID **included** |
| Dundee | 30, 120, 240 | |

**Models in Hussain reproduction**: `dl_baseline_lstm`, `dl_baseline_transformer`, `dl_baseline_hybrid`

---

## Dataset Configurations

### Our splits (`hussain=False`)

| Dataset | Train range | Test range | COVID exclude |
|---------|------------|-----------|---------------|
| ACN_Caltech | 2018-04-25 → 2021-01-17 | 2021-01-17 → 2021-09-14 | 2020-08-05 → 2020-11-17 |
| ACN_JPL | 2018-10-09 → 2021-01-17 | 2021-01-17 → 2021-09-14 | 2020-08-05 → 2020-11-17 |
| ACN_Office001 | 2019-03-25 → 2020-07-31 | 2020-08-21 → 2021-09-14 | — |
| Dundee | 2021-07-27 → 2023-09-01 | 2023-09-01 → 2025-08-30 | — |
| BeLib | 2017-04-01 → 2017-04-24 | 2017-04-25 → 2017-04-30 | — |
| AMB_Barcelona | 2018-12-31 → 2019-10-19 | 2019-10-20 → 2019-12-31 | — |

### Hussain splits (`hussain=True`)

| Dataset | Train range | Test range | COVID exclude |
|---------|------------|-----------|---------------|
| ACN_Caltech | 2018-04-25 → 2020-08-05 | 2020-11-17 → 2021-09-14 | **none** (COVID included) |
| ACN_JPL | 2018-10-09 → 2020-08-05 | 2020-11-17 → 2021-09-14 | **none** |
| Dundee | 2021-07-27 → 2023-09-01 | 2023-09-01 → 2025-08-30 | — |

---

## Look-Back / Horizon Constants

| Constant | Value | Applies to |
|----------|-------|-----------|
| `LOOK_BACK_DAYS` | 28 | All our neural models (LSTM, Transformer, Hybrid) — fixed, decoupled from horizon |
| `MIN_LOOK_BACK` | 14 | Floor for Hussain variants: `look_back = max(14, forecast_horizon)` |

**Rationale for `LOOK_BACK_DAYS=28`**: Captures two full weekly cycles and
covers the most significant ACF lags (7, 14, 28 days) without anchoring
predictions to a stale demand regime at long horizons.

---

## Experiment Hyperparameters (TFM defaults)

| Model | Key params | Transforms |
|-------|-----------|-----------|
| LSTM | epochs=100, bs=32, lr=0.001, dropout=0.2, activation='relu' | log1p |
| Transformer | epochs=100, bs=32, lr=0.001, head_size=128, num_heads=4, ff_dim=4, n_blocks=2, dropout=0.1 | log1p |
| Hybrid | epochs=100, bs=32, lr=0.001, d_model=128, num_heads=4, dropout=0.1 | log1p |
| LightGBM | num_leaves=31, lr=0.02, max_depth=8, early_stopping=200 | none |
| XGBoost | random_state=16, test_size=0.20 | none |
| Hussain LSTM | same as LSTM + `use_lr_scheduler=True`, `use_early_stopping=True` | log1p |
| Hussain Transformer | encoding_dim=64, num_heads=4, key_dim=64, dropout=0.2 + schedulers | log1p |
| Hussain Hybrid | d_model=128, num_heads=4, dropout=0.2 + schedulers | log1p |

All experiments: `use_differencing=False`, `use_calendar_features=False` (default in TFM runs).

---

## Evaluation Window Shrinkage Warning

For `h=120` with `look_back=28`: effective eval days ≈ `len(test) − look_back`.
On ACN datasets (test ≈ 240 days after COVID exclusion), this leaves ~212 eval days — adequate.
On ACN Hussain test (test ≈ 300 days), eval days ≈ 270 days.

Flag cases where `len(test) − look_back < 30` (statistically fragile).
